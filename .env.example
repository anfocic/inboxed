# Server Configuration
PORT=3000
NODE_ENV=development

# Logging Configuration
LOG_LEVEL=info
# LOG_FILE=./logs/error.log  # Optional: Log errors to file

# SMTP Configuration (Required)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password

# Email Destination (Required)
TO_EMAIL=recipient@example.com

# CORS Configuration
# In development, you can use "*" to allow all origins
# In production, specify exact domains: "https://yourdomain.com,https://www.yourdomain.com"
ALLOWED_ORIGINS=*

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000# 15 minutes in milliseconds
RATE_LIMIT_MAX=100# Max requests per window per IP

# File Upload Configuration
MAX_FILE_SIZE=5242880# 5MB in bytes
MAX_FILES=5# Maximum number of files per submission
UPLOAD_DIR=/tmp# Directory for temporary file storage
ALLOWED_FILE_TYPES=image/jpeg,image/png,image/webp# Comma-separated MIME types

# Security (Optional)
# TRUST_PROXY=true           # Set to true if behind a reverse proxy
